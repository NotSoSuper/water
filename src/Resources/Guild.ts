import BaseResource from "../Abstracts/BaseResource";
import GuildRepresentation from "../Representations/Guild";

export default class Guild extends BaseResource implements GuildRepresentation {
    
}